# Local Update Server Configuration

# Update Mode: 'auto' or 'github'
# - auto: Auto-discover local server via UDP broadcast, fallback to GitHub (recommended)
# - github: Force use of GitHub only (skip server discovery)
# Default: auto
UPDATE_MODE=auto
